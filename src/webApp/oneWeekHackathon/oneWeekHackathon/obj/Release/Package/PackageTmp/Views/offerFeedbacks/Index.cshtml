@model IEnumerable<oneWeekHackathon.Models.offerFeedback>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.customer.userTag)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.offer.offerTag)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.review)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.customer.userTag)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.offer.offerTag)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.review)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.offerFeedbackId }) |
            @Html.ActionLink("Details", "Details", new { id=item.offerFeedbackId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.offerFeedbackId })
        </td>
    </tr>
}

</table>
